import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const JSX = () => {
  return (
    <View>
          <Text>WE CAN NOT USE STATEMENTS INSIDE JSX BUT WE CAN 
              USE EXPRESSIONS.
              EXPRESSIONS are those  which return some value
              and the STATEMENTS are those which do not return any value.
      </Text>
    </View>
  )
}

export default JSX

const styles = StyleSheet.create({})